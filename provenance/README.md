# Provenance

This directory documents the relationship between the original analysis notebook and the cleaned public scripts used in this repository.

The public scripts are derived from manuscript-relevant notebook cells. Personal Google Drive paths, Colab-only commands, notebook-state dependencies, and redundant superseded steps were removed for public repository use.

See `notebook_cell_mapping.csv` for the mapping between notebook cells and public scripts.
